{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Bush 41 Declassified Memcon",
  "type": "object",
  "required": [
    "id",
    "date",
    "type",
    "title",
    "participants",
    "countries",
    "chapter",
    "releaseStatus",
    "naid",
    "catalogUrl",
    "frusVolume",
    "frusTopics",
    "source"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable local identifier, preferably date plus participant slug."
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "type": {
      "type": "string",
      "enum": ["Memcon"]
    },
    "title": {
      "type": "string"
    },
    "participants": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "countries": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "chapter": {
      "type": "object",
      "required": ["number", "name"],
      "properties": {
        "number": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4
        },
        "name": {
          "type": "string",
          "enum": ["United Kingdom", "France", "Italy", "Regional"]
        }
      }
    },
    "sortDate": {
      "type": "string",
      "format": "date",
      "description": "Date used for chronological ordering within the assigned chapter."
    },
    "releaseStatus": {
      "type": "string",
      "enum": ["Full", "Partial", "Denied", "Unknown"]
    },
    "naid": {
      "type": "string"
    },
    "pdfUrl": {
      "type": "string",
      "format": "uri"
    },
    "catalogUrl": {
      "type": "string",
      "format": "uri"
    },
    "source": {
      "type": "object",
      "required": ["name", "url"],
      "properties": {
        "name": {
          "type": "string"
        },
        "url": {
          "type": "string",
          "format": "uri"
        },
        "foiaNumber": {
          "type": "string"
        }
      }
    },
    "frusVolume": {
      "type": "object",
      "required": ["id", "title", "url", "status"],
      "properties": {
        "id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "url": {
          "type": "string",
          "format": "uri"
        },
        "status": {
          "type": "string"
        }
      }
    },
    "frusTopics": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "topics": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "notes": {
      "type": "string"
    }
  }
}
